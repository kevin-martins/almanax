import { FilterSearch } from "./filter"

export type AlmanaxProps = {
    bonus: string,
    date: string,
    name: string,
    quantity: number,
    icon: string,
    filterSearch: string,
    reward: number,
}
